{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-dialog",
  "description": "Allow only the main window to use the fixed native dialog baseline.",
  "windows": ["main"],
  "permissions": ["dialog:default"]
}
