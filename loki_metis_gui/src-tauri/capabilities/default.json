{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "LokiMetis application-window capability with the fixed native dialog baseline.",
  "windows": [
    "main",
    "pet",
    "pet-settings"
  ],
  "permissions": [
    "core:default",
    "core:event:default",
    "dialog:default"
  ]
}
