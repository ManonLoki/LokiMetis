{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "LokiMetis main-window capability with the fixed native dialog baseline.",
  "windows": [
    "main"
  ],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}

