{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "pet-settings-drag",
  "description": "Allow the borderless pet settings window to drag itself via its own header.",
  "windows": ["pet-settings"],
  "permissions": ["core:window:allow-start-dragging"]
}
